spring:
  application:
    name: eureka-server
server:
  port: 1112

eureka:
  instance:
    hostname: peer2
  client:
    service-url:
      defaultZone: http://localhost:1111/eureka/ # 将注册中心地址指向peer1